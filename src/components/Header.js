const Header = () => {
    return (
        <header className="header">
            <h1>Tracker App</h1>
            <button className="btn">Add</button> 
        </header>
    )
}

export default Header
